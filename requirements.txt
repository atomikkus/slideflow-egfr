# =============================================================================
# EGFR Driver Mutation Classifier — Python dependencies
# =============================================================================
# Install via setup.sh (recommended) — it handles ordering, CUDA index URL,
# --no-deps flag for slideflow, and source patches.
#
# Manual install order (if not using setup.sh):
#   1. pip install setuptools==70.2.0
#   2. pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 \
#          --index-url https://download.pytorch.org/whl/cu124
#   3. pip install slideflow==3.0.2 --no-deps
#   4. pip install -r requirements.txt
#   5. Apply patches (see setup.sh or CLAUDE.md)
# =============================================================================

# --- Core ---
# NOTE: torch/torchvision are installed separately with CUDA index URL
# torch==2.6.0+cu124
# torchvision==0.21.0+cu124

slideflow==3.0.2

# --- MIL trainer (SlideFlow dependency, not pulled in by --no-deps) ---
fastai==2.8.7
fastcore==1.12.38
fastprogress==1.1.5
ipython==9.12.0

# --- Feature extraction ---
timm==1.0.26
huggingface-hub==1.10.1
transformers==5.5.3

# --- SlideFlow missing deps ---
fpdf2==2.8.7
rasterio==1.5.0
scikit-image==0.26.0
shapely==2.1.2
zarr==3.1.6
tabulate==0.10.0
opencv-python-headless==4.13.0.92

# --- Data / ML ---
numpy==2.4.3
pandas==3.0.2
scikit-learn==1.8.0
scipy==1.17.1
h5py==3.16.0
pyarrow==23.0.1
umap-learn==0.5.12

# --- Visualization ---
matplotlib==3.10.8
seaborn==0.13.2

# --- GCS ---
google-cloud-storage==3.10.1

# --- Misc ---
tqdm==4.67.3
packaging==26.0
setuptools==70.2.0
